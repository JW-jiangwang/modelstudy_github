day01：用tokenizer观察文本和token之间的变化

day02：embedding 和余弦相似度，验证语义检索能区分同词不同义

day03：理解封装一个类，该类是一个简单的检索功能，并且能够直观的看到字面相近，语意不同时容易出现近邻噪声

day04：使用chroma来替换暴力检索，可以替换后的代码更加简洁

day05：RAG检索：用chroma进行检索，然后调用LLM进行根据文档进行回答，其中是用promat将文档和问题拼接起来，还需进行一个幻觉测试

day06：写RAG评估代码，对于企业来说不仅仅是要RAG检索，更重要的是RAG检索效果怎么样，所以要计算检索正确率、答案准确率、拒绝编造率

day07：同一组测试，用两个Embedding模型对比，明白模型不是越大越好，合适最重要